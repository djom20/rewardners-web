<%= simple_form_for(payment) do |f| %>
  <%= f.error_notification %>
  <div class="form-inputs">
    <%= f.input :plan_id, collection: [["Month $5 USD", 1]] %>
  </div>
  <div class="form-actions">
    <%= f.button :submit, class: "btn btn-primary" %>
  </div>
<% end %>
